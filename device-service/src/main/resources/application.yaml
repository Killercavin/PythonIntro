spring:
  application:
    name: device-service
  datasource:
    url: jdbc:mariadb://localhost:3306/wattwise
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
server:
  port: 8080