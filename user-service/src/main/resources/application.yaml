spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mariadb://localhost:3306/wattwise
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
server:
  port: 8080